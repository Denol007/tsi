[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-campus-assistant"
version = "2.0.0"
description = "AI-powered TSI Campus Assistant Telegram Bot"
requires-python = ">=3.10"
